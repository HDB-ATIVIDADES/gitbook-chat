services:
  gitbook-chat:
    image: node:20-alpine
    container_name: gitbook-chat
    ports:
      - "8000:8000"
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
    command: node server.js
    restart: unless-stopped
